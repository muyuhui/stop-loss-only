## 1. 数据模型与迁移

- [x] 1.1 新增资产级 `price_history` 模型，包含代码、资产类型、交易日期、价格、来源和抓取时间，并建立组合唯一约束与查询索引
- [x] 1.2 增加幂等数据库迁移和版本升级测试，确保现有 SQLite 持仓、告警及设置数据保持不变
- [x] 1.3 为历史行情查询、去重和多个同代码持仓复用补充模型层测试

## 2. 历史行情获取与缓存

- [x] 2.1 调研并验证 AkShare 股票日线与目标开放式基金历史净值接口，记录字段、日期、复权和异常返回差异
- [x] 2.2 实现统一历史行情适配器，将股票收盘价和基金单位净值规整为按市场日期升序的 Decimal 记录
- [x] 2.3 实现按 `1m`、`3m`、`6m`、`1y` 计算日期范围、检测缓存覆盖及仅抓取缺失区间的服务
- [x] 2.4 使用事务和 upsert 写入历史缓存，保证相同代码、类型和交易日期不会产生重复记录
- [x] 2.5 实现外部请求失败时返回已有缓存与过期状态、无缓存时返回可恢复错误及关联标识
- [x] 2.6 为股票、基金、空结果、重复数据、增量刷新和外部失败回退补充服务测试

## 3. 止损序列与历史接口

- [x] 3.1 基于现有止损引擎实现固定价格和百分比止损的逐日水平序列
- [x] 3.2 实现以买入价为初始最高价、按日期更新运行最高价的移动止损阶梯序列
- [x] 3.3 标记价格小于或等于当日止损价的触发点，并明确输出采用当前止损配置的计算语义
- [x] 3.4 新增 `GET /api/holdings/{id}/history` 接口及响应模型，校验持仓、范围和升序去重契约
- [x] 3.5 为四种时间范围、买入日过滤、三类止损线、触发点、404、422、缓存过期和错误关联标识补充 API 测试

## 4. 前端图表基础能力

- [x] 4.1 安装并按需配置 Apache ECharts 所需模块，确认生产构建没有引入完整未使用组件
- [x] 4.2 新增历史行情 API 调用和独立请求状态，确保图表加载或失败不阻塞持仓详情主体
- [x] 4.3 实现 `HoldingPriceChart` 组件，展示价格折线、买入价虚线、水平或阶梯止损线、风险区域和触发标记
- [x] 4.4 为图表增加不依赖颜色的图例与文字摘要，显示最高价、最低价、最新价、止损价、来源和最后交易日期
- [x] 4.5 实现画布尺寸响应、销毁清理和窗口变化处理，防止重复实例与监听器泄漏

## 5. 持仓详情交互

- [x] 5.1 将价格走势面板放入风险摘要与止损设置之间，默认查询近 3 个月且不改变现有详情操作层级
- [x] 5.2 实现 1 月、3 月、6 月和 1 年范围切换，切换期间保留已有图表并防止过期响应覆盖新选择
- [x] 5.3 接入首次加载、空数据、缓存过期、刷新失败和重试反馈，并标注“止损线按当前规则计算”
- [x] 5.4 优化 360px 到 767px 手机布局、触控提示和高度，保证没有页面级横向滚动或重要文字截断
- [x] 5.5 为范围切换、竞态保护、缓存提示、空状态、止损图例和响应式结构补充前端单元测试

## 6. 验证与文档

- [x] 6.1 使用真实股票和基金数据验证首次抓取、缓存复用、增量更新以及固定、百分比、移动止损图形
- [x] 6.2 在 360px、390px、768px、1024px 和 1280px 视口验证图表可读性、触控/鼠标提示和详情操作可达性
- [x] 6.3 运行后端测试、前端单元测试、生产构建、包体预算、冒烟检查和 OpenSpec 严格校验并修复所有回归
- [x] 6.4 更新 README，说明历史行情来源、缓存行为、止损线语义、支持范围和故障排查方式
