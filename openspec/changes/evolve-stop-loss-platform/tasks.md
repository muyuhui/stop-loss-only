## 1. 基线与发布安全

- [ ] 1.1 为无数据、活动持仓、已触发、已关闭、重复代码和告警历史等场景准备具有代表性的 schema v2 数据库，且不得提交个人数据。
- [ ] 1.2 为现有持仓、仪表盘、告警、设置和刷新响应增加 API 契约 fixture。
- [ ] 1.3 增加测试网络哨兵，在所有必选后端测试和冒烟测试中立即拒绝外部 DNS 或 socket 访问。
- [ ] 1.4 配置 pytest 和冒烟测试使用项目范围内明确可写的临时目录，并在完成后清理。
- [ ] 1.5 增加回归测试，证明 fixture 价格刷新不会导入或调用真实交易日历与行情提供方。
- [ ] 1.6 在修改生产行为前，为关闭持仓收益展示和并发重复触发增加回归测试。
- [ ] 1.7 增加默认关闭的 `STOP_LOSS_V2_MODE` 功能开关，并记录其支持的取值。
- [ ] 1.8 运行现有 26 项后端测试、11 项前端测试、生产构建、OpenSpec 严格校验和修复后的离线冒烟测试，作为基线门禁。

## 2. 行情与交易日历可信层

- [ ] 2.1 使用 Decimal 值定义内部 `QuoteState`、`NormalizedQuote`、`MarketSessionStatus` 和稳定的提供方错误码类型。
- [ ] 2.2 定义支持注入时钟的 `QuoteProvider` 与 `MarketCalendar` 协议，并禁止适配器之外的代码依赖 DataFrame 类型。
- [ ] 2.3 实现确定性的 fixture 行情和交易日历适配器，并为全部行情状态编写单元测试。
- [ ] 2.4 重构 akshare 股票适配器，将原始列映射为规范化行情，并增加有界超时与脱敏错误处理。
- [ ] 2.5 重构 ETF、LOF 和开放式基金净值适配器，实现资产特定的行情状态与新鲜度策略。
- [ ] 2.6 实现带过期时间、最近成功值回退和明确降级状态的交易日历缓存。
- [ ] 2.7 实现周期级数据集缓存，确保每个全市场数据集在单周期内最多下载一次。
- [ ] 2.8 为提供方调用增加有限重试、带抖动的退避和短时熔断状态。
- [ ] 2.9 扩展行情 API schema，增加状态、来源、行情/抓取/新鲜截止时间、可行动性和稳定错误码。
- [ ] 2.10 将新持仓迁移为 `unpriced` 语义，同时保留买入价作为明确标识的成本占位值。
- [ ] 2.11 增加提供方契约 fixture，覆盖列重命名或缺失、空数据集、非法价格、延迟数据和节假日净值行为。
- [ ] 2.12 在启用网络哨兵的情况下运行提供方单元测试和集成测试。

## 3. 监控周期与事务性触发

- [ ] 3.1 增加 `monitoring_cycles` 迁移、ORM 模型、索引和保留字段。
- [ ] 3.2 持久化调度、手动、跳过、部分成功和失败周期摘要，且不保存敏感值。
- [ ] 3.3 实现 `/api/monitoring/status`，返回调度器状态、下次运行、最近周期、最近成功、覆盖率和降级原因。
- [ ] 3.4 实现带稳定筛选和错误码的分页 `/api/monitoring/cycles` 诊断接口。
- [ ] 3.5 将现有告警生命周期键替换为稳定的仓位/规则/触发序列幂等键。
- [ ] 3.6 为触发状态转换增加乐观版本条件，并且只根据已提交记录构造响应。
- [ ] 3.7 使用保存点隔离每个标的的持久化，避免一个行情失败静默回滚无关结果。
- [ ] 3.8 增加进程内或数据库支持的周期互斥，避免手动与调度全量刷新重复下载行情。
- [ ] 3.9 实现可按标的或仓位 ID 限定范围的手动刷新，同时保留兼容刷新路由。
- [ ] 3.10 增加并发测试，覆盖触发事务胜出/失败、唯一约束冲突、部分提交和数据库忙等结果。
- [ ] 3.11 验证监控日志和 API 错误包含关联 ID/周期 ID，但不包含价格、数量、成本或原始响应。
- [ ] 3.12 在开始领域模型迁移前运行完整 P0 离线门禁。

## 4. 有序迁移与影子领域模型

- [ ] 4.1 选择 Alembic，或说明同等有序迁移执行器更合适的原因，并将决策固化到项目文档。
- [ ] 4.2 为标的、账户、仓位、交易批次、平仓分配、止损规则和仓位事件增加有序迁移。
- [ ] 4.3 为行情快照、监控周期、扩展告警快照和通知投递尝试增加迁移及必要索引。
- [ ] 4.4 配置默认本地账户，以及由市场/代码/类型构成的标的身份唯一性规则。
- [ ] 4.5 实现 schema v2 数据迁移，把每条 holding 转换为一个标的、仓位、初始批次和活动或历史规则。
- [ ] 4.6 将已关闭持仓转换为平仓分配，并保留原平仓价、状态、时间戳和告警快照。
- [ ] 4.7 为迁移后的活动、已触发和已关闭生命周期事实生成初始不可变事件。
- [ ] 4.8 构建自动对账报告，对比迁移前后的数量、成本、状态、止损价、告警数和仪表盘汇总。
- [ ] 4.9 在所有代表性 schema v2 fixture 上测试升级、声明支持的降级和迁移中断恢复。
- [ ] 4.10 增加影子读取，对比旧模型与新模型汇总，但不改变 API 响应。
- [ ] 4.11 对账失败时保持 `STOP_LOSS_V2_MODE` 关闭，并暴露不含敏感信息的 readiness 原因。
- [ ] 4.12 在启用任何新模型写入前，通过迁移数据完整性和备份恢复门禁。

## 5. 仓位核算与生命周期服务

- [ ] 5.1 实现基于 Decimal 的批次创建、剩余数量汇总和默认 FIFO 成本分配。
- [ ] 5.2 实现部分与全部平仓命令，处理数量精度、平仓时间、费用、税费和净已实现收益。
- [ ] 5.3 实现当前估值、未实现收益、仓位权重、止损风险金额、预计止损损失和覆盖率公式。
- [ ] 5.4 实现版本化止损规则启用，并确保移动止损最高价属于当前有效规则生命周期。
- [ ] 5.5 实现带 payload schema 版本和可注入时钟的不可变仓位事件。
- [ ] 5.6 实现风险确认，且不改变告警阅读状态或触发事实。
- [ ] 5.7 实现重新布防，要求原因、新规则、新触发序列和明确的即时风险确认。
- [ ] 5.8 部分平仓后要求用户决定剩余仓位规则，并拒绝含糊的部分平仓提交。
- [ ] 5.9 禁止修改已关闭仓位的财务数据，但允许带审计记录的非财务元数据更新。
- [ ] 5.10 增加完整的核算和状态机测试，覆盖基金小数份额、费用、税费、多批次、部分平仓和舍入边界。
- [ ] 5.11 在功能开关后将创建、加仓、刷新、触发、确认、重新布防和平仓写入切换到新服务。
- [ ] 5.12 每次切换写路径后重新运行新旧模型对账，仅在所有契约一致时移除双写。

## 6. 版本化 API 与兼容性

- [ ] 6.1 使用 Decimal 安全序列化和明确的行情/估值覆盖字段定义新版仓位列表与详情 DTO。
- [ ] 6.2 实现 `/api/positions` 的搜索、生命周期、资产类型、行情状态、风险筛选、排序和稳定分页。
- [ ] 6.3 实现仓位详情、增加批次、部分/全部平仓、风险确认和重新布防接口，并返回稳定错误结构。
- [ ] 6.4 实现仓位历史接口，支持分页事件和有界图表序列。
- [ ] 6.5 扩展告警 API，增加处置筛选、事件链接及确认/重新布防/平仓关系。
- [ ] 6.6 扩展仪表盘 API，增加监控可信度、估值覆盖率、仓位权重、风险金额和预计止损损失指标。
- [ ] 6.7 在一个兼容周期内，把新模型映射回现有 `HoldingResponse`、仪表盘兼容字段和当前路由。
- [ ] 6.8 为未取价、过期、部分刷新、已触发、重新布防、部分关闭和已关闭状态增加 OpenAPI 示例。
- [ ] 6.9 增加契约测试，证明启用 v2 模式时旧响应字段仍然可用且映射正确。
- [ ] 6.10 记录新路由、稳定错误码、Decimal 编码和未来旧接口弃用流程。

## 7. 前端状态基础与设计系统

- [ ] 7.1 扩展监控健康、行情状态、业务风险、财务盈亏和中性表面的语义设计变量，且状态不能只依赖颜色。
- [ ] 7.2 增加共享数字格式化工具，支持千分位、Decimal 字符串、带符号百分比、数量精度和不可用值。
- [ ] 7.3 增加可访问的共享组件，用于行情状态标签、监控状态、风险标签、过期数据提示条和紧凑空状态/筛选空状态。
- [ ] 7.4 增加可复用筛选工具栏，将搜索、筛选、排序和页码与路由查询参数同步。
- [ ] 7.5 为监控、仓位和告警创建 Pinia store，包含最近成功数据、请求去重和明确的后台刷新状态。
- [ ] 7.6 合并应用级轮询，使监控摘要和未读计数各自只有一个定时器，并根据页面可见性安全调整频率。
- [ ] 7.7 增加组件测试，覆盖服务端状态保留、过期时间、查询恢复、权限状态和无重复定时器。
- [ ] 7.8 在迁移业务页面前，于 390x844、768x1024 和 1440x900 视口验证共享组件。

## 8. 仪表盘与监控界面

- [ ] 8.1 增加紧凑监控状态条，展示市场状态、最近成功时间、覆盖率、失败数量和诊断操作。
- [ ] 8.2 无风险时用紧凑行动摘要替换过大的安全风险主面板，有风险时显示明确的行动面板。
- [ ] 8.3 增加净已实现收益、估值覆盖率、高风险敞口和预计止损损失等组合指标。
- [ ] 8.4 为风险持仓增加行情状态、仓位权重、绝对收益和预计止损损失，同时保留风险优先排序。
- [ ] 8.5 增加带周期分页和脱敏提供方失败详情的监控诊断页面或抽屉。
- [ ] 8.6 增加加载、部分成功、过期、调度器停用和无可行动行情状态，且不得清空最后成功数据。
- [ ] 8.7 为每种仪表盘可信度和风险组合增加真实挂载的组件测试。
- [ ] 8.8 在所有要求视口完成浏览器截图、溢出、键盘操作和固定导航检查。

## 9. 仓位列表、详情与生命周期界面

- [ ] 9.1 将持仓列表请求替换为新版 positions API，并提供搜索、生命周期、资产、行情和风险筛选。
- [ ] 9.2 实现桌面风险表格，支持可配置次要列和稳定数字对齐。
- [ ] 9.3 实现紧凑且可展开的移动端卡片，始终显示当前价、盈亏、止损距离和行情状态。
- [ ] 9.4 围绕可信行情、绝对/百分比盈亏、止损风险和预计损失重新设计活动仓位详情。
- [ ] 9.5 增加交易批次历史和加仓表单，并支持资产特定的价格与数量精度。
- [ ] 9.6 增加部分/全部平仓表单，包含数量、价格、日期、费用、税费、核算结果预览和确认。
- [ ] 9.7 为已触发仓位增加处置条，提供风险确认、重新布防和记录平仓流程。
- [ ] 9.8 增加重新布防表单，要求填写原因，并展示新规则预览和即时风险确认。
- [ ] 9.9 实现关闭仓位复盘模式，展示净已实现收益、费用、持有时长和触发到平仓滑点，且不显示活动止损控件。
- [ ] 9.10 为未取价、过期、已触发、已确认、重新布防、部分关闭和已关闭视图增加真实挂载的组件测试。
- [ ] 9.11 完成创建、加仓、刷新、触发、确认、重新布防、部分平仓和全部平仓的浏览器 E2E。

## 10. 告警处置与历史界面

- [ ] 10.1 增加告警阅读状态/处置状态/日期/标的/类型筛选，并同步 URL 和提供真实分页控件。
- [ ] 10.2 将规则快照、行情状态、风险金额、阅读状态和处置状态作为不同字段展示。
- [ ] 10.3 将每条告警链接到对应仓位事件，并为已删除仓位保留可用快照。
- [ ] 10.4 增加风险确认、重新布防和平仓操作，不得与标记已读操作混淆。
- [ ] 10.5 分别提供无告警、筛选无匹配和无待处理告警的空状态。
- [ ] 10.6 增加多页数据及并发标记已读/风险确认操作的组件测试。
- [ ] 10.7 验证桌面端扫描密度和完整移动端卡片，确保没有横向滚动。

## 11. 仓位历史与数据保留

- [ ] 11.1 持久化规范化的可行动行情快照、有效止损线值和周期 ID。
- [ ] 11.2 实现带索引的事件分页和历史时间范围查询。
- [ ] 11.3 实现服务端下采样，最多返回 1000 个点，同时保留触发、规则变更和平仓关键点。
- [ ] 11.4 仅在记录包体、可访问性和维护成本权衡后选择轻量图表方案。
- [ ] 11.5 增加仓位价格/止损线图，显示规则、触发和平仓标记，并提供文字回退摘要。
- [ ] 11.6 增加不可变事件时间线，显示操作者、原因和易理解的事件标签。
- [ ] 11.7 实现可配置的行情/诊断保留策略、分批清理任务和存储索引。
- [ ] 11.8 增加下采样边界、关键点保留、分批清理及图表空状态/错误状态测试。

## 12. 设置与通知投递

- [ ] 12.1 将设置 API 和界面拆分为监控、行情/历史、通知和数据/诊断分组。
- [ ] 12.2 保留及时/均衡/省资源预设，并显示实际生效值和调度器状态。
- [ ] 12.3 实现密钥写入、替换和清除存储，且不得返回完整密钥值。
- [ ] 12.4 通过用户明确操作启用浏览器通知，并处理已授权、已拒绝和不支持状态。
- [ ] 12.5 实现主事务提交后的投递尝试创建，并保证通道/幂等键唯一性。
- [ ] 12.6 实现 HTTPS Webhook 签名、最小默认 payload、超时、有限重试和安全错误日志。
- [ ] 12.7 增加 SSRF 目标校验，并单独记录允许本地目标的开发覆盖配置。
- [ ] 12.8 增加通知通道测试操作，且不得修改告警事实或监控设置。
- [ ] 12.9 增加测试，证明投递失败不会回滚仓位/告警，密钥不会出现在响应或日志中。
- [ ] 12.10 在所有要求视口验证分组设置和移动端表单。

## 13. 导入、导出与用户运维

- [ ] 13.1 定义带版本的标准持仓 CSV 模板，使用 Decimal 安全列并记录编码格式。
- [ ] 13.2 实现有界 CSV 解析、规范化和公式注入防护。
- [ ] 13.3 实现预览令牌、逐行校验结果和预览阶段零写入行为。
- [ ] 13.4 实现对已确认且未过期预览的原子提交，并生成导入领域事件。
- [ ] 13.5 为活动/关闭仓位、批次、规则和核算值实现稳定 CSV 导出。
- [ ] 13.6 在设置页增加导入预览、确认、逐行错误和导出流程。
- [ ] 13.7 增加仅限配置备份目录的 UI 备份操作，并展示 checksum/schema 结果。
- [ ] 13.8 实现隐私友好的诊断包预览/导出，默认不包含数据库或密钥。
- [ ] 13.9 增加往返导入导出、非法输入、超大文件、过期令牌和公式注入测试。

## 14. 数据库运维与恢复

- [ ] 14.1 在生产引擎工厂中启用并测试 SQLite 外键、WAL 和有界 busy timeout。
- [ ] 14.2 确保备份名称不会冲突，并在 manifest 中包含应用/schema 版本和 WAL 感知的完整性信息。
- [ ] 14.3 替换前校验恢复文件的 checksum、manifest 格式、支持的 schema 范围、源完整性和目标路径。
- [ ] 14.4 恢复前创建当前数据库恢复点，并在替换后的 readiness 失败时自动还原。
- [ ] 14.5 增加迁移对账、历史清理、数据库大小报告和隐私友好诊断命令。
- [ ] 14.6 更新启动/恢复脚本和 README，说明功能开关、新迁移、WAL 文件和回滚边界。
- [ ] 14.7 测试同秒多次备份、不兼容 manifest、损坏 WAL 状态和 readiness 失败时的备份恢复行为。

## 15. 最终验证与分阶段发布

- [ ] 15.1 在阻断网络的情况下运行全部领域、API、迁移、提供方、并发、导入和运维测试。
- [ ] 15.2 运行真实挂载的 Vue 组件测试以及生产构建/包体预算检查。
- [ ] 15.3 在 390x844、768x1024 和 1440x900 运行浏览器 E2E，并捕获溢出、重叠和文本截断失败。
- [ ] 15.4 在每个代表性数据库上运行 schema v2 迁移对账，并验证回滚文档。
- [ ] 15.5 运行 OpenSpec 严格校验，并确认每条已实现需求至少关联一项自动化测试。
- [ ] 15.6 在复制的本地数据库上启用 v2 模式，运行完整离线监控/生命周期流程并检查隐私安全日志。
- [ ] 15.7 更新 README、API 文档、设置帮助和故障排查，覆盖行情可信度、处置、部分平仓和诊断。
- [ ] 15.8 记录已知限制：不连接券商、不自动下单、免费数据源延迟、本地单用户支持和暂不支持的高级策略。
- [ ] 15.9 仅在一个稳定版本发布且验证旧路由使用情况后，通过独立兼容性清理变更移除功能开关回退。
