## 1. DeepSeek 契约与安全基础

- [x] 1.1 对照实现时最新 DeepSeek 官方 API 文档确认固定 HTTPS 端点、默认模型、鉴权、结构化 JSON 参数、错误响应和超时语义，并把模型名与有界超时加入应用配置而不开放自定义 Base URL
- [x] 1.2 增加版本化复盘快照、事实项、Provider 结构化结果、公开 API 响应和稳定错误的 Pydantic 契约，限定动作枚举、字段长度、条目数量与额外字段策略
- [x] 1.3 定义业务无关的 `AIProvider` 接口和第一版只注册 DeepSeek 的 Provider registry，使复盘服务不依赖供应商请求格式
- [x] 1.4 扩展本机密钥存储与设置响应，支持 DeepSeek Key 的保存、原子替换、清除和仅返回 configured 状态，证明密钥不进入 SQLite 或普通设置

## 2. 近期行情与确定性事实

- [x] 2.1 抽取可复用的历史行情缺口补齐与缓存读取逻辑，在不改变现有持仓图表买入日起点的前提下实现独立 90 天、最多 60 个有效点的 AI 分析窗口
- [x] 2.2 实现历史行情新鲜度、来源、最后交易日、缓存降级、无数据和少于 20 点样本的稳定判定与错误原因
- [x] 2.3 使用 Decimal 安全计算 5 日、20 日、60 日收益、最大回撤、区间高低位置和不可用指标，并为持仓、止损、行情与风险预算生成稳定事实注册表
- [x] 2.4 增加近期行情与事实快照单元测试，覆盖新买入持仓的买入前行情、缓存缺口、提供方失败降级、样本不足、空历史、shadow-read 权威选择和不完整风险覆盖

## 3. DeepSeek 适配与复盘服务

- [x] 3.1 实现 DeepSeek Provider 的最小结构化请求、低随机性、有界连接/总超时、响应解析和鉴权/限流/超时/不可用错误映射，禁止记录请求体、响应体、Authorization 或原始供应商错误
- [x] 3.2 实现复盘服务：校验活动 Holding 与当前行情、构造快照、调用 Provider、校验事实引用、用权威事实生成最终响应，并应用已触发止损、数据不足、风险覆盖和加仓能力的确定性覆盖规则
- [x] 3.3 增加单进程按 Holding 的有界并发保护和 `ai_review_busy`，确保异常、取消和超时后锁可靠释放
- [x] 3.4 新增 `POST /api/ai/holdings/{holding_id}/review` 与 DeepSeek 最小连接检测 API，接入统一能力守卫、关联 ID 和稳定错误响应，review 路由不得自行运行监控周期
- [x] 3.5 增加离线 Provider/API/数据库测试，覆盖合法结构、非法 JSON、额外字段、无效事实引用、禁止动作、错误矩阵、连接检测、零业务写入和仅允许 `price_history` 缓存更新

## 4. 设置与运行时能力

- [x] 4.1 为 legacy 与 shadow-read 增加 `ai_holding_reviews=true`，为不受支持的 new-authoritative 保持 false，并同步后端能力策略、响应 fixture、前端 store 默认值和直接路由守卫
- [x] 4.2 扩展设置 API，提供 DeepSeek Key 的配置、替换、清除与 configured 状态，确保失败时保留先前有效密钥且读取永不回显
- [x] 4.3 在设置页增加 DeepSeek 专区，展示云端数据发送范围、未配置/已配置状态、密钥输入、替换、确认清除和连接检测，不展示其他 Provider、自定义 Base URL 或自动交易设置
- [x] 4.4 增加设置与能力测试，覆盖两种稳定权威阶段、不受支持阶段、密钥存储不可用、检测失败不清除、秘密不进入响应/日志/诊断/备份，以及未配置时的前端引导

## 5. 持仓详情复盘体验

- [x] 5.1 在活动 Holding 详情增加“更新行情并 AI 复盘”入口，按顺序执行现有目标行情刷新、重新读取 Holding 和 review API，并显示三个阶段且阻止重复提交
- [x] 5.2 实现结构化复盘区域，展示受限动作、结论、权威事实依据、风险情景、数据局限、可信程度、行情时间、历史最后交易日、生成时间和模型标识，所有模型内容使用纯文本渲染
- [x] 5.3 实现未配置、行情未就绪、历史不可用、超时、限流、无效响应和并发占用的可理解降级与重试；DeepSeek 失败时保留原持仓、止损与手动平仓能力
- [x] 5.4 仅在最终动作、风险覆盖和 `risk_plan_previews` 同时允许时展示进入现有加仓风险试算的入口，并明确它不等于推荐买入、不携带数量且不产生写入
- [x] 5.5 增加真实组件测试，覆盖配置引导、分阶段顺序、成功结果、已触发止损覆盖、页面离开后结果失效、重复提交保护、错误重试和加仓能力守卫

## 6. 端到端门禁与文档

- [x] 6.1 扩展完全离线 fixture 后端与网络哨兵，为 AkShare 历史和 DeepSeek 提供确定性响应，保证强制测试不会访问 DNS、socket 或真实 Provider
- [x] 6.2 在 `390x844`、`768x1024` 和 `1440x900` 完成设置状态、单持仓刷新、成功复盘、Provider 失败和受限加仓入口浏览器 E2E，检查溢出、遮挡、文本截断和浏览器错误
- [x] 6.3 增加可识别测试密钥的全链路隐私断言，并验证成功、拒绝和失败复盘前后所有核心业务表与关键字段保持不变
- [x] 6.4 更新 README、设置说明、API/schema fixture 和运行排障文档，明确 DeepSeek 数据发送范围、近期行情口径、建议限制、密钥存储、错误码和可选真实连接检测
- [x] 6.5 连续运行两次完整 `verify.ps1` 与全部 OpenSpec 严格校验，修复任何后端、前端、构建、包体、进程清理、E2E、隐私或规格回归
