## 1. 后端可信契约

- [x] 1.1 更新 `HoldingResponse` 和 legacy 展示载荷，使未定价且依赖行情的百分比允许为空；随后更新已定价与未定价持仓的 API、OpenAPI 和冻结契约测试。
- [x] 1.2 修改监控覆盖率计算：活动持仓分母为空时返回可空值，存在活动持仓时保留真实的 0%；补充两种情况及兼容别名的状态 API 测试。
- [x] 1.3 扩展 `GET /api/alerts`，支持有长度上限的快照搜索、明确的 true/false 未读筛选、经过校验的处置状态筛选，以及分页前稳定计算的总数与顺序；补充分页和历史空处置状态测试。
- [x] 1.4 新建 legacy 告警时写入 `triggered` 处置状态；手动关闭持仓时，以原子方式把相关待处理告警更新为 `closed`，并通过测试证明阅读状态和告警快照保持不变。

## 2. 前端行为与界面

- [x] 2.1 让共享百分比格式化、数值色调、止损风险分类和仪表盘排序安全处理空值；补充工具测试，证明未知值不会变成零、临近止损或安全状态。
- [x] 2.2 更新持仓列表、持仓详情和仪表盘，显示明确的“未定价”/“风险未知”标签；未知距离排在已知风险之后；当所有活动持仓距离均未知时，显示风险待定摘要。
- [x] 2.3 将告警工具栏精简为仅保留 URL 驱动的搜索、阅读状态和处置状态控件；每个可见筛选条件都发送到服务端，当前值和默认值使用中文；补充真实组件测试，覆盖重置、从 URL 恢复、分页及筛选结果为空。
- [x] 2.4 当 `risk_budget_reads` 不可用时隐藏仪表盘风险预算区域，同时保留受保护的规划器直达路由，并避免请求可选风险 API。
- [x] 2.5 按可用风险能力控制设置页风险政策区域；仅保存监控设置时不提交隐藏的风险字段，以保留已有政策值；补充能力不可用、能力请求失败和能力可用三种状态的组件测试。
- [x] 2.6 更新仪表盘覆盖率文案：覆盖率为空时表达“暂无活动持仓”或可信证据不可用，真实零覆盖率仍明确显示为 `0%`。

## 3. 回归覆盖与交付

- [x] 3.1 更新基线 API/模式样例和 README 契约说明，涵盖可空持仓指标、空组合覆盖率及稳定告警查询参数。
- [x] 3.2 扩充真实挂载组件测试，覆盖未定价持仓在列表/详情/仪表盘中的状态、实际生效的告警控件，以及因能力不可用而隐藏的仪表盘/设置区域。
- [x] 3.3 扩展 `390x844`、`768x1024` 和 `1440x900` 浏览器流程，断言不存在虚假的零风险、无效或英文告警筛选、能力关闭的风险区域、页面溢出及浏览器错误。
- [x] 3.4 运行后端测试、前端行为/组件测试、生产构建与包体预算、三个视口的端到端测试、离线冒烟测试、恢复演练及严格 OpenSpec 校验；在受支持的宿主环境中连续运行两次 `verify.ps1`，确认进程、端口或临时目录残留不会影响第二次执行。
