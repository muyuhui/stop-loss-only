## 1. 数据模型与迁移

- [x] 1.1 定义金额精度、行情元数据、`triggered`/`closed` 状态、告警快照及生命周期约束。
- [x] 1.2 编写可回滚的 SQLite 迁移：先备份，再转换金额、映射 `stopped_out`、回填告警快照。
- [x] 1.3 验证迁移前后行数、金额、状态、约束以及备份恢复结果。

## 2. 交易日历与行情适配器

- [x] 2.1 实现日期归一化、Asia/Shanghai 早盘/午盘时段判断和可观测的交易日历退化模式。
- [x] 2.2 重构 akshare 访问：每类市场数据每轮最多获取一次，场外净值每个唯一代码最多一次。
- [x] 2.3 统一行情结果字段：资产类型、代码、价格、来源、行情时间、抓取时间、新鲜度和结构化错误。
- [x] 2.4 增加交易日格式、时段边界、午休、节假日、同代码多持仓、部分失败和旧行情测试。

## 3. 统一监控与止损生命周期

- [x] 3.1 实现供调度器和手动刷新共用的事务型监控周期服务。
- [x] 3.2 将一条行情分发到全部匹配持仓，仅用成功且新鲜的行情更新最高价。
- [x] 3.3 按统一 Decimal 舍入策略改造止损计算与阈值比较。
- [x] 3.4 原子完成 `holding → triggered`，并保证重试和并发刷新不会产生重复告警。
- [x] 3.5 实现 `triggered → closed` 的显式确认，分别保存触发价和实际平仓价。

## 4. 集成与兼容

- [x] 4.1 用统一监控服务替换调度器和手动刷新中的重复循环。
- [x] 4.2 更新持仓、仪表盘和告警序列化，支持新状态、行情元数据、定点金额和告警快照。
- [x] 4.3 在迁移期处理旧状态兼容，并同步更新前端状态标签与操作。

## 5. 验证

- [x] 5.1 增加新鲜触发、旧/失败行情、重复代码、并发刷新、删除后告警历史和确认平仓的集成测试。
- [x] 5.2 在代表性数据库副本上执行迁移，并验证备份与回滚流程。
- [x] 5.3 运行后端测试和前端构建，人工检查交易时段边界上的定时与手动刷新。
