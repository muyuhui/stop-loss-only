## 1. 统一 API 契约

- [x] 1.1 定义分页、持仓详情/列表、仪表盘、刷新周期、告警摘要和运行时设置的 Pydantic schema。
- [x] 1.2 建立共享持仓响应构建器和计算不变量，保证各接口返回同一快照值。
- [x] 1.3 生成并检查 OpenAPI 中的状态码、校验范围、可空字段和 Decimal 序列化。

## 2. 对齐持仓 API

- [x] 2.1 在新建和止损修改时计算、保存并一致返回止损价。
- [x] 2.2 为列表增加 page、size、status 校验、稳定排序和 `{items,total,page,size}`。
- [x] 2.3 落实生命周期相关的修改、平仓和删除规则；删除成功返回无响应体的 204，并保留告警快照。
- [x] 2.4 增加字段一致性、分页边界、筛选、原子校验、状态流转、404 和删除语义集成测试。

## 3. 仪表盘与刷新契约

- [x] 3.1 实现当前敞口、未实现收益、毛已实现收益和各生命周期数量。
- [x] 3.2 增加 Asia/Shanghai 今日告警数和最新告警，确保持仓字段与持仓 API 一致。
- [x] 3.3 序列化统一监控周期结果，包含周期元数据、逐标的结果、部分失败和关联 ID。
- [x] 3.4 覆盖空/混合组合、日期边界、最新告警排序、无持仓及刷新成功/部分失败/致命失败测试。

## 4. 运行时设置

- [x] 4.1 增加服务端范围校验，以及“运行时应用成功后再提交”的设置服务。
- [x] 4.2 调度器启动前读取已保存间隔，应用失败时恢复先前运行状态。
- [x] 4.3 前端轮询器使用配置间隔、响应设置变化，并保证每类轮询最多一个计时器。
- [x] 4.4 覆盖默认值、持久化、非法值、重启恢复、动态调整和卸载清理测试。

## 5. 前端迁移与验证

- [x] 5.1 更新持仓、仪表盘、设置、告警和手动刷新页面，适配规范响应与新状态。
- [x] 5.2 明确展示部分刷新和旧行情，不把失败标的显示为已刷新。
- [x] 5.3 运行后端、前端测试与构建，并用生成的 OpenAPI 验证响应示例。
