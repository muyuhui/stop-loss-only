## 1. 权威风险事实与预算计算

- [x] 1.1 抽取统一的权威风险事实结构和适配器：`legacy`/`shadow-read` 只读取活动 Holding，`new-authoritative` 保留 Position 读取；使用 Decimal 计算单条当前风险并标记覆盖缺口。
- [x] 1.2 重构风险预算聚合以消费统一风险事实，补充 legacy 完整覆盖、已触发仍占用、关闭后释放、覆盖不完整、空组合及 shadow 投影不重复计入的服务测试。
- [x] 1.3 保持现有 Position 风险预算公式和隔离 API 行为兼容，补充按 authority 选源及不可行动行情不影响止损风险覆盖的回归测试。

## 2. 新仓与加仓只读试算 API

- [x] 2.1 将新仓数量计算整理为无数据库写入的纯 Decimal 路径，按当前 authority 风险预算支持 legacy/shadow-read，并返回取整前后数量、试算后风险和稳定拒绝原因。
- [x] 2.2 新增加仓试算请求/响应 schema 和纯计算函数，实现单笔剩余容量与组合剩余容量双重约束、现有止损价保留、费用扣除、资产单位向下取整及试算后风险指标。
- [x] 2.3 新增 `POST /api/risk/plans/add-on-preview`，校验 Holding 存在且状态为 `holding`，为已触发/已关闭、计划价不高于止损、风险覆盖不完整和容量不足返回稳定拒绝响应。
- [x] 2.4 更新 OpenAPI 与冻结 API/schema fixture；通过 API 集成测试证明成功和拒绝的新仓/加仓试算均不修改 Holding、Position、批次、规则、事件、告警、设置或行情记录。

## 3. 能力策略与稳定前端流程

- [x] 3.1 在 `legacy` 和 `shadow-read` 启用 `risk_budget_reads` 与 `risk_plan_previews`，继续关闭 Position 创建及生命周期写入；更新能力、readiness、直接写入拒绝和 shadow-read 集成测试。
- [x] 3.2 解耦前端的试算与创建能力：规划器按 `risk_plan_previews` 可见，Position 创建确认仅在 `risk_covered_position_creation` 可用时出现，并验证稳定运行面不会请求任何写入 API。
- [x] 3.3 将规划器实现为 URL 可恢复的“新仓试算”与“加仓试算”模式，完成中文表单、加载/拒绝/成功状态、风险约束最大数量、费用、加仓前后持仓与组合风险及试算时间展示。
- [x] 3.4 在状态为 `holding` 的详情页增加“加仓风险试算”入口；只用可行动行情预填计划价，未定价或不可行动时保持为空，并对 triggered、closed 和能力不可用状态隐藏入口。
- [x] 3.5 更新仪表盘风险预算与设置页风险政策在稳定能力开启后的可见和错误状态，确保组合权益未设置、覆盖不完整及请求失败不会显示虚假零值或可执行建议。

## 4. 可信文案与交付门禁

- [x] 4.1 更新 README 和界面文案，统一使用“风险试算”“风险约束下的最大数量”，明确不判断标的、不预测收益、不验证券商现金、不预留容量、不下单且不记录实际加仓。
- [x] 4.2 扩充后端单元/API 测试和真实挂载 Vue 测试，覆盖股票/基金取整、费用耗尽、单笔容量先耗尽、组合容量先耗尽、止损边界、URL 恢复、行情预填及零写入保证。
- [x] 4.3 扩展 `390x844`、`768x1024` 和 `1440x900` 浏览器流程，完成新仓与加仓试算，并断言没有 Position/批次写入、自动下单或“推荐标的”命令，没有页面横向溢出和浏览器错误。
- [x] 4.4 运行后端测试、前端行为/真实组件测试、生产构建与包体预算、三视口 E2E、离线冒烟、恢复演练及全部 OpenSpec 严格校验；连续运行两次 `verify.ps1`，确认没有端口、进程或临时目录残留影响第二次执行。
