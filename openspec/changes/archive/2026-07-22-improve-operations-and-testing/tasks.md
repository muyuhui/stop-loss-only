## 1. 配置、依赖与应用生命周期

- [x] 1.1 增加数据库路径、监听地址、端口、调度器开关、日志模式和超时的类型化配置。
- [x] 1.2 固定运行依赖，拆分测试/开发依赖，并提供显式 setup 命令。
- [x] 1.3 重构应用工厂和 lifespan，显式初始化/关闭数据库、调度器与日志，移除导入时 schema 修改。
- [x] 1.4 增加数据库、schema revision 和调度器状态的 liveness/readiness 检查。

## 2. 迁移与恢复

- [x] 2.1 引入迁移工具，建立当前 schema 基线，并接入前两个变更需要的 revision。
- [x] 2.2 实现迁移状态、升级和降级命令；schema 不兼容时启动失败并给出操作提示。
- [x] 2.3 实现带 checksum、schema manifest 和完整性校验的时间戳 SQLite 备份。
- [x] 2.4 实现仅在服务停止时执行的恢复流程，先校验临时副本再可恢复替换。
- [x] 2.5 使用代表性旧版/新版数据库自动测试迁移和备份恢复。

## 3. 安全本地进程管理

- [x] 3.1 拆分并文档化 setup、正常启动、开发 reload、停止、备份、恢复和验证脚本。
- [x] 3.2 默认监听 loopback、关闭 reload，端口占用时报告但不终止占用者。
- [x] 3.3 记录进程所有权元数据，只有 PID、命令和工作区均匹配时才停止进程。
- [x] 3.4 开发 reload 默认禁用调度器，正常模式强制单一所有者并验证优雅关闭。
- [x] 3.5 覆盖端口占用、PID 复用、依赖缺失、就绪超时、正常停止和所有权清理测试。

## 4. 可观测性

- [x] 4.1 为请求、监控周期、调度器、迁移和启动增加关联 ID 与结构化日志。
- [x] 4.2 增加日志脱敏测试，保证默认不记录价格、数量、响应体和敏感配置。
- [x] 4.3 文档化日志位置/保留策略，以及 readiness、行情失败、迁移和调度器排障方法。

## 5. 自动化质量门禁

- [x] 5.1 建立临时数据库、注入时钟、行情 fixture、交易时段和并发场景的隔离测试基建。
- [x] 5.2 扩展后端单元/集成测试，覆盖 API、监控生命周期、迁移、调度器、设置和失败路径。
- [x] 5.3 增加前端测试工具与响应处理、状态 UI、部分失败、动态轮询和计时器清理测试。
- [x] 5.4 增加隔离端到端冒烟流程：启动、等待就绪、执行新建-刷新-触发-关闭-仪表盘、清理自有进程。
- [x] 5.5 提供一个非交互验证命令，逐项报告门禁，任一必选项失败即返回非零。

## 6. 前端构建与最终验证

- [x] 6.1 将 Element Plus 和图标改为按需导入，保留路由懒加载。
- [x] 6.2 定义入口和路由包体预算，报告产物大小，超出预算时构建失败。
- [x] 6.3 在准备好的干净环境运行完整离线验证，并文档化独立的真实行情冒烟步骤。
