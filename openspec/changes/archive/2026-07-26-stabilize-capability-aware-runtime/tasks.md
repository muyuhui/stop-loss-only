## 1. 统一运行时能力策略

- [x] 1.1 定义类型明确的后端能力模型和唯一权威阶段策略，覆盖稳定运行时支持、旧持仓写入、风险规划、Position 生命周期写入、CSV 和 Webhook
- [x] 1.2 新增只读接口 `GET /api/runtime/capabilities`，验证响应只包含稳定且不敏感的权威阶段与功能元数据
- [x] 1.3 让就绪检查和权威阶段相关的路由守卫复用统一策略，同时不启用 `new-authoritative` 运行时支持
- [x] 1.4 使用真实应用增加后端测试，覆盖 `legacy`、`shadow-read` 和不受支持的 `new-authoritative` 的能力与就绪状态组合

## 2. 让前端感知运行时能力

- [x] 2.1 增加类型明确的运行时能力 API 和启动状态；能力发现失败时，可选功能默认关闭
- [x] 2.2 根据能力控制规划器导航和仪表盘风险预算轮询，并在稳定页面保留清晰的不可用说明
- [x] 2.3 当规划能力不可用时，为直接访问 `/planner` 的用户展示稳定说明，并阻止预览或仓位写入请求
- [x] 2.4 增加预期能力冲突的逐请求分类，让受保护视图抑制通用错误提示，同时不隐藏意外故障
- [x] 2.5 增加前端测试，覆盖能力发现成功/失败、隐藏规划器导航、不发起风险轮询、直接路由降级和意外错误可见性

## 3. 修正监控语义

- [x] 3.1 扩展监控响应模型，增加交易日历感知的新鲜度以及独立的可操作/估值覆盖率，同时保留 `overdue` 和 `quote_coverage_pct` 别名
- [x] 3.2 根据权威交易日历和周期证据计算 `market_closed`、`healthy`、`overdue` 和 `no_success`，未知覆盖率不得表示为零
- [x] 3.3 更新仪表盘和监控界面，以中性方式展示休市，保留上次成功时间，并明确区分两种覆盖率
- [x] 3.4 增加确定性的前后端测试，覆盖开市期间过期、权威休市、保留估值价格和覆盖率未知

## 4. 保证恢复工作文件清理

- [x] 4.1 将恢复工作副本删除放入保证执行的收尾路径，同时在所有失败分支保留活动数据库和恢复点
- [x] 4.2 工作副本清理失败时，返回稳定且不暴露敏感信息的运维错误
- [x] 4.3 增加恢复成功、替换前失败、替换/复制后失败和清理失败测试；清理成功的路径均断言不遗留工作数据库
- [x] 4.4 清除本地测试工作区中的旧恢复工作文件，并确保验证流程不会再次遗留此类文件

## 5. 验证集成后的稳定运行时

- [x] 5.1 增加或更新浏览器测试，证明旧版稳定界面不会展示或调用不支持的规划命令，也不会产生通用能力错误提示
- [x] 5.2 运行后端测试、前端测试、生产构建、浏览器 E2E、冒烟检查、恢复演练和严格 OpenSpec 校验
- [x] 5.3 再次运行完整质量门禁，确认第一次运行产生的端口、浏览器进程、测试数据库和恢复工作文件不会影响第二次运行
